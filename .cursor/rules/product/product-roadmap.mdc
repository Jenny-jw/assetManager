---
description: Product roadmap P0–P4 (ADR 005 SaaS)
alwaysApply: true
---

# Product Roadmap

`[ ]` todo · `[~]` wip · `[x]` done

## P0 — Foundation

| #   | Item                                                      | St  |
| --- | --------------------------------------------------------- | --- |
| 1   | ADRs (001–005)                                            | [x] |
| 2   | `product` branch                                          | [x] |
| 3   | `deploy/config.yaml` + loader (`locale`, modules)         | [x] |
| 4   | `core/capabilities.py`                                    | [x] |
| 5   | PostgreSQL: SQLAlchemy + Alembic + compose `postgres`     | [x] |
| 6   | `schemas/`, initial migration (`users`, `stocks`)         | [x] |

## P1 — SaaS tenancy (ADR 005)

| #   | Item                                                                                                 | St  |
| --- | ---------------------------------------------------------------------------------------------------- | --- |
| 5a  | ADR 005 + rules aligned (ADR files remain local/gitignored by policy)                                | [x] |
| 5b  | Expand schema: `tenants` + nullable transition `tenant_id` FK on `users`, `stocks` (Alembic)          | [x] |
| 5c  | Tenant resolution (JWT `tenant_id`, scoped `get_current_user`)                                       | [x] |
| 5d  | Signup creates tenant + owner; login resolves tenant (`slug` + username); trial lifecycle             | [x] |
| 5e  | `GET /api/deployment` from current tenant (not process-global YAML)                                  | [x] |
| 5f  | Register all module routers; per-request `module_disabled` (supersedes P1 #8 startup omission)       | [x] |
| 5g  | Multi-tenant isolation tests (tenant A ≠ tenant B)                                                   | [x] |
| 5h  | Contract schema: backfill/verify tenant ownership, then make all `tenant_id` columns `NOT NULL`       | [x] |

## P1 — Personal edition

| #   | Item                                                                                                 | St  |
| --- | ---------------------------------------------------------------------------------------------------- | --- |
| 7   | Personal preset as default (`deploy/config.yaml` = personal; `orders: false`)                        | [x] |
| 8   | ~~`main.py` conditional routers~~ → **5f** per-request module checks (ADR 005)                       | [x] |
| 9   | Owner auth on PG                                                                                     | [x] |
| 10  | Inventory API on PG — see **P1-10 breakdown**; cleanup after → `product-branch-cleanup.mdc`          | [x] |
| 11  | Dashboard widget registry — see **P1-11 breakdown** below                                            | [x] |
| 12  | Frontend routes gated by modules — no Orders nav/pages on personal                                   | [x] |
| 13  | i18n (`react-i18next`) — load **one** locale from tenant/deployment at startup                       | [x] |
| 14  | Personal edition tests — capabilities, orders off, widgets, stock API (pytest + Vitest)              | [x] |

### P1-11 — Dashboard widget registry (incremental)

| Sub | Item | St |
|-----|------|-----|
| 11a | `core/dashboard_widgets.py` — widget IDs, module mapping, `effective_dashboard_layout()` | [x] |
| 11b | `schemas/product/deployment.py` — API response DTO (`modules`, `dashboard_layout`, `locale`, `edition`) | [x] |
| 11c | `GET /api/deployment` + `router_registry` (subset of P2 #16; no capabilities yet) | [x] |
| 11d | Frontend `types/Deployment.ts` + `deploymentServices.ts` | [x] |
| 11e | `DeploymentContext` — load config once at app startup | [x] |
| 11f | Frontend widget registry (`widget id` → component) | [x] |
| 11g | `Dashboard.tsx` — render `dashboard_layout` only; drop `isAdmin` gating for `pending_orders` | [x] |
| 11h | Dashboard actions for `owner` (Add Asset / Manage Inventory); defer nav gating to **#12** | [x] |
| 11i | Vitest — personal layout hides `PendingOrdersInbox` | [x] |

**Deferred:** full `capabilities` in deployment API → **P2 #16**; i18n labels → **#13**; stock API on frontend (`/api/stock`) → align with P1-12 or follow-up.

**Deferred from P1-9** (other roadmap items):

| Concern | Roadmap | Notes |
| ------- | ------- | ----- |
| Frontend username login; trial signup (new tenant + owner) | **#12–13, 5d** | Open signup creates a **new tenant**; not global single-owner gate |
| Structured error codes (`signup_disabled`, …) | **#17** | P1-9 uses HTTP status + `detail` string; formalize in P2 |
| Owner password reset (email or token flow) | **#26** | See P4 below |
| Auth rate limiting; HTTPS cookie hardening in prod | **#24** | Compose prod profile + SaaS deploy docs |

## P2 — Structure

| #   | Item | St  |
| --- | ---- | --- |
| 15  | `repositories/postgres/` — tenant-scoped `StockRepository` (no raw SQL in routes) | [x] |
| 16  | `GET /api/deployment` — edition, modules, capabilities, `locale`, `dashboard_layout` | [ ] |
| 17  | Structured API error codes (`module_disabled`, `capability_disabled`, `trial_expired`, …) | [ ] |
| 18  | `modules/{inventory,orders,analytics}/` package layout; routes thin | [ ] |

## P3 — Professional edition (owner-only)

| #   | Item | St  |
| --- | ---- | --- |
| 19  | Professional preset wiring (`orders`, `profit_analytics` on) at tenant signup | [ ] |
| 20  | Owner-managed orders on PG (`orders` / `order_items` / `stock_movements` migration) | [ ] |
| 21  | Cost models + `view_profit` / pricing analytics | [ ] |
| 22  | Order states (pending → approve/reject); stock decrement on approve | [ ] |

## P4 — Polish & delivery

| #   | Item | St  |
| --- | ---- | --- |
| 23  | SaaS ops docs (trial, subscription status, compose prod, `.env`) | [ ] |
| 24  | Compose prod profile | [ ] |
| 26  | Owner password reset + admin recovery | [ ] |
| 27  | Payment provider hook for subscription (Stripe or local equivalent) — optional | [ ] |

## Future — customer-facing

| — | `customer` role; catalog browse; place orders / trades | not started |

## Out of scope / deferred

| Item | Why |
|------|-----|
| External / legacy DB import into SaaS | Tenants start empty via self-service signup |
| Multi-DB import (N buyer PGs → one PG) | No independent-deploy buyers to merge (ADR 005) |
| Fleet / per-buyer Docker + dedicated PG | Optional data-sovereignty tier later; not default delivery |

**Next:** P2 **#16** (`GET /api/deployment` includes capabilities). **This version:** `owner` only on all editions.
